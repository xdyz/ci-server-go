basePath: /api/v1
info:
  contact: {}
  description: 后端基础架构api文档
  title: 后端基础架构api文档
  version: "1.0"
paths:
  /user:
    get:
      consumes:
      - application/json
      description: do ping
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: ping example
      tags:
      - example
swagger: "2.0"
